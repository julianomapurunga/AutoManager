node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.*
!.env.example
uploads/

# Material gerado por outras ferramentas (kits de marca etc.) — não é do projeto
output/

# Config local de ferramenta (Claude Code) — versiona só as skills do projeto
.claude/*
!.claude/skills/
